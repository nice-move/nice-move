{
  "name": "linter-config",
  "version": "0.0.0",
  "description": "linter config for personal use",
  "license": "UNLICENSED",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=8.3.0"
  },
  "optionalDependencies": {
    "ava": "^1.4.1",
    "eslint": "^5.16.0",
    "eslint-plugin-node": "^8.0.1",
    "husky": "^1.3.1",
    "lint-staged": "^8.1.5",
    "prettier": "^1.17.0",
    "stylelint": "^9.10.1"
  },
  "scripts": {
    "format": "lint-staged",
    "test": "ava --verbose"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  }
}
