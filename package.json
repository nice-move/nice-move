{
  "private": true,
  "name": "nice-move",
  "version": "0.0.0",
  "description": "Eslint/Stylelint/Prettier config for personal use",
  "license": "UNLICENSED",
  "files": [],
  "scripts": {
    "format": "nice-move",
    "postinstall": "node ./config/hot-fix.js",
    "test": "ava --verbose"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "eslint-find-rules": "^3.4.0",
    "fs-chain": "^0.0.2",
    "stylelint-find-new-rules": "^1.2.0"
  },
  "optionalDependencies": {
    "ava": "^2.4.0",
    "eslint": "^6.7.2",
    "husky": "^3.1.0",
    "prettier": "^1.19.1",
    "stylelint": "^12.0.0"
  },
  "engines": {
    "node": "^8.15 || ^10.15 || ^12"
  },
  "eslintConfig": {
    "extends": "@nice-move/eslint-config-base"
  },
  "husky": {
    "hooks": {
      "pre-commit": "nice-move && ava"
    }
  },
  "prettier": "@nice-move/prettier-config",
  "stylelint": {
    "extends": "@nice-move/stylelint-config"
  }
}
